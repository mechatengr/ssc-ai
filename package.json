{
  "name": "ssc-ai-android",
  "version": "1.0.0",
  "private": true,
  "description": "Capacitor wrapper that packages the SSC AI web app as a native Android app.",
  "scripts": {
    "cap:add-android": "cap add android",
    "cap:sync": "cap sync android",
    "cap:build": "cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/core": "^6.1.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  }
}
